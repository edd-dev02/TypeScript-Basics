// Tipo texto
const name: string = "Eduardo";

// Tipo número o el valor exacto "FULL"
let hpPoints: number | "FULL" = 95;
hpPoints = "FULL";

// Tipo booleano
const isAlive: boolean = true;

console.log({name, hpPoints, isAlive});

export {};